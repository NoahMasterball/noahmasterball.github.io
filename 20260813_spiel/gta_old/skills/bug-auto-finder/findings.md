# Bug Finder — Confirmed Findings

Entries below were investigated and CONFIRMED by adversarial verification.
They are automatically skipped on future runs unless the referenced files changed since the finding date.

When a finding's referenced files change, it is re-verified:
- If the bug is now fixed → remove the entry
- If the bug still exists → update the date
- If the bug changed shape → update description + date

---
